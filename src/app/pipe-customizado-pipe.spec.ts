import { PipeCustomizadoPipe } from './pipe-customizado-pipe';

describe('PipeCustomizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCustomizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
